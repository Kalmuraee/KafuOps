apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-kafuops-config
  labels:
    {{- include "kafuops.labels" . | nindent 4 }}
data:
  .kafuops.yml: |
{{ toYaml .Values.config | indent 4 }}
